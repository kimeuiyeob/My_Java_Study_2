셋은 우선적으로
인덱스 정보를 포함하고 있지 않다!!
셋은 그냥 주머니에 순서없이 집어넣은 느낌이라고 보면된다.
중복저장은 불가능하다.
인덱스 정보가 없기 대문에 특정 위치 값을 꺼낼수가 없다.
		
이래서 먼가 문제풀때 중복검사할때 set을 쓰면 간편하게 풀수있다.
		
예를 들면 셋은 값을 꺼낼때 인덱스 위치로 꺼내는게아니라
해당 값으로 꺼내는데 만약 중복저장이 된다면
어떤걸 꺼낼지 모르니까 중복이 저장안되서
안녕을꺼내오면 안녕이 나온다. 
		
Set

HashSet<E>
-hashCode(), equals()를 통해 중복 체크
-입력 순서 != 출력순서

LinkedHashSet<E>
-HashSet의 기본 기능 포함
-입력순서 == 출력순서

TreeSet
-Set<E>의 기본 기능
-정렬,검색 기능 추가
-출력순서는 오름차순